<div id="content">
  <h1>Listing schools</h1>
  Can't find your school? <%= link_to "Add it", new_school_path %>!
  <p>
    <% @schools.each do |school| %>
      <%= link_to school.name, school %> <%= link_to "enroll", school_enroll_path(school) %>
      <!--%= link_to 'Edit', edit_school_path(school) %-->
      <!--%= link_to 'Destroy', school, :confirm => 'Are you sure?', :method => :delete %-->
    <% end %>
  </p>
</div>